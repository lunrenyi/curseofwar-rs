[package]
name = "cow-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Curse of War (1.3.0) Rust re-implementation core: zero UI/IO deps, just RNG and game logic."

[dependencies]
rand = { workspace = true }